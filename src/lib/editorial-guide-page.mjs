import { editorialGuides } from "../content/editorial-guides.mjs";
import { absolute, escapeHtml, icon, layout, pageHero } from "./template.mjs";

export function guideImage(article, { eager = false, card = false } = {}) {
  const sizes = card ? "(max-width: 640px) calc(100vw - 40px), (max-width: 960px) 45vw, 400px" : "(max-width: 880px) calc(100vw - 40px), 830px";
  return `<picture><source type="image/webp" srcset="/assets/guides/${article.image}-640.webp 640w, /assets/guides/${article.image}-1200.webp 1200w" sizes="${sizes}"><img src="/assets/guides/${article.image}.jpg" width="1200" height="800" alt="${escapeHtml(article.alt)}" loading="${eager ? "eager" : "lazy"}"${eager ? ' fetchpriority="high"' : ""} decoding="async"></picture>`;
}

export function editorialGuideCards({ compact = false } = {}) {
  return `<section id="housing-reading" class="guide-reading" aria-labelledby="housing-reading-title"><div class="section-header"><div><p class="eyebrow">한지붕 주거정보 · 새 글 5편</p><h2 id="housing-reading-title">공간을 살피고,<br>서로의 생활을 이해하는 시간</h2></div><p>어르신의 빈방, 청년의 주거 선택, 함께 지킬 생활규칙.<br>현장의 질문을 조사와 세대교류로 이어갑니다.</p></div><div class="guide-card-grid${compact ? " guide-card-grid-compact" : ""}">${editorialGuides.map((article, index) => `<article class="guide-reading-card">${guideImage(article, { card: true })}<div class="guide-card-copy"><p class="resource-meta">0${index + 1} · ${escapeHtml(article.category)} · ${article.readTime}</p><h3><a href="${article.href}">${escapeHtml(article.title)}</a></h3><p>${escapeHtml(article.description)}</p><a class="text-link" href="${article.href}">글 읽기${icon("arrow")}</a></div></article>`).join("")}</div></section>`;
}

export function editorialGuidePage(article) {
  const related = article.related.map(href => editorialGuides.find(item => item.href === href));
  const body = pageHero({ eyebrow: article.category, title: escapeHtml(article.title), description: article.description, meta: `<div class="page-meta"><span>작성 한지붕 운영팀</span><span>읽기 ${article.readTime} · 예상 시간</span><span>게시·수정 <time datetime="${article.updatedAt}">${article.updatedAt.replaceAll("-", ".")}</time></span></div>` }) +
    `<section class="section guide-article-section"><div class="container content-layout"><article class="prose guide-article"><figure class="guide-photo">${guideImage(article, { eager: true })}<figcaption>${escapeHtml(article.caption)}</figcaption></figure><div class="resource-summary"><h2>이 글을 읽고 나면</h2><p>${escapeHtml(article.outcome)}</p></div><nav class="resource-toc" id="guide-contents" aria-label="글 목차"><h2>차례</h2><ol>${article.sections.map(section => `<li><a href="#${section.id}">${escapeHtml(section.title)}</a></li>`).join("")}</ol></nav><section class="guide-takeaways"><h2>먼저 기억할 세 가지</h2><ul>${article.points.map(point => `<li>${escapeHtml(point)}</li>`).join("")}</ul></section>${article.sections.map(section => `<section aria-labelledby="${section.id}"><h2 id="${section.id}">${escapeHtml(section.title)}</h2>${section.body}</section>`).join("")}<section class="guide-next"><h2>내게 필요한 다음 단계</h2><div class="hero-actions">${article.actions.map(([label, href], index) => `<a class="button${index ? " button-secondary" : ""}" href="${href}">${escapeHtml(label)}</a>`).join("")}</div><p>한지붕은 기초상담을 전화·이메일로 무료 안내합니다. 입주자 연결·계약대행은 제공하지 않습니다.</p></section><div class="reader-actions"><div class="reader-buttons"><button type="button" class="print-button" data-print>${icon("print")}이 글 인쇄하기</button><a class="text-link" href="#guide-contents">목차로 돌아가기</a></div></div><section class="source-panel"><h2>작성 범위</h2><p>한지붕 운영팀이 생활과 주거에 관해 나눌 질문을 정리한 자체 안내글입니다. 개별 주택의 적합성, 계약·법률·세무 판단이나 전문가 검수를 대신하지 않습니다. 실제 활동과 조사 진행 현황은 각 기록 페이지에서 별도로 확인할 수 있습니다.</p></section><section class="related-section"><h2>이어서 읽어보세요</h2><div class="related-grid">${related.map(item => `<a class="related-link" href="${item.href}"><span class="resource-meta">${escapeHtml(item.category)}</span>${escapeHtml(item.title)}</a>`).join("")}</div></section></article><aside class="side-nav"><h2>필요한 정보로 바로가기</h2><a href="/resources/#housing-reading">주거정보 글 5편</a><a href="/resources/preparation-room/">내 상황에 맞는 준비 질문표</a><a href="/programs/housing-research/">조사 진행상황</a><a href="/activities/">실제 활동기록</a><a href="/transparency/">운영·투명성</a></aside></div></section>`;
  return layout({ title: article.title, description: article.description, path: article.href, body, bodyClass: "article-page guide-page", type: "article", publishedAt: article.publishedAt, updatedAt: article.updatedAt, breadcrumbs: [{ label: "주거정보", href: "/resources/" }, { label: article.title, href: article.href }], jsonLd: { "@context": "https://schema.org", "@type": "Article", headline: article.title, description: article.description, datePublished: article.publishedAt, dateModified: article.updatedAt, author: { "@type": "Organization", name: "한지붕" }, mainEntityOfPage: absolute(article.href) } });
}
